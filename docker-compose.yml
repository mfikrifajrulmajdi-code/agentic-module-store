version: '3.8'

services:
  kasku-ai:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - HMAC_SECRET=kasku_dev_secret_key_2026
    restart: always
